'use strict'

export const doubledValues = (numbers) => {

  const value = numbers.map(x => x * 2);
  return value;

/*  const result = []

  for (let i = 0; i < numbers.length; i++) {
    result.push(numbers[i] * 2)
  }

  return result
*/
}
